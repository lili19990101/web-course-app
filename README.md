#Web Course Project's README

## How to install and run

Run the following commands

```
npm install
npm run dev
open http://localhost:3000/ in broswer

```

You only need to run `npm install` at the first time.